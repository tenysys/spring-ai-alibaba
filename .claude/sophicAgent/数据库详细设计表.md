### **10.2.1** **账号权限域**

**users****（****已有****）**

作用：平台基础用户主表。

对应功能：登录认证、用户身份识别、资源归属。

表关系及说明：

    关联关系：

1. 主键被user_info.user_id、user_additional_info.user_id、

user_login.user_id、user_sign.user_id 关联。

2. 主键被 sa_api_key.user_id、sa_session.user_id、sa_agent_feedback.user_id 等业务表引用。

关系说明：登录认证、用户身份识别、资源归属需要和其关联的 user_info.user_id、user_additional_info.user_id、user_login.user_id、user_sign.user_id、sa_api_key.user_id保持一致性。

索引说明：

ID：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|ID|varchar(128)|PK|用户主键|
|NAME|varchar(64)|UQ|用户名|
|PASSWORD|varchar(256)||密码|
|ENABLED|decimal(1)||是否启用|
|TYPE|decimal(1)||用户类型|
|SALT|varchar(32)||密码盐值|

**user_info****（****已有****）**

作用：用户基础资料表。

对应功能：用户资料展示、组织归属、联系方式。

表关系及说明：

关联关系：引用users.ID做主键。

关系说明：用户资料展示、组织归属、联系方式需要和其关联的 users.ID保持一致性。

索引说明：

USER_ID：主键索引，用于单条记录精确定位和主从关联。

phone_index：手机号普通索引，用于按手机号查询用户资料。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|USER_ID|varchar(128)|PK, FK|用户 id|
|PHONE|varchar(32)|IDX|手机号|
|E_MAIL|varchar(100)||邮箱|
|POSITION|varchar(100)||岗位|
|DESCRIPTION|varchar(100)||描述|
|ORG_ID|varchar(128)||组织 id|
|ORDERNUM|decimal(8)||排序号|
|CREATE_TIME|datetime||创建时间|
|PWD_CHANGE_TIME|datetime||密码修改时间|

**user_additional_info****（****已有****）**

作用：用户扩展资料表。

对应功能：个人补充信息、头像、附件、额外属性。

表关系及说明：

关联关系：引用users.ID做主键。

关系说明：个人补充信息、头像、附件、额外属性需要和其关联的 users.ID保持一致性。

索引说明：

user_id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|user_id|varchar(128)|PK, FK|用户 id|
|person_name|varchar(32)||姓名|
|aornum|varchar(64)||工号/编号|
|nationality|varchar(32)||国籍|
|sex|int(1)||性别|
|card_type|varchar(16)||证件类型|
|id_card|varchar(32)||证件号|
|create_time|datetime||创建时间|
|major|int(1)||专业|
|annex|varchar(32)||附件|
|avatar|varchar(32)||头像|
|additional_info|varchar(4096)||扩展信息|
|source|varchar(10)||来源|

**user_login****（****已有****）**

作用：用户登录流水表。

对应功能：登录态审计、在线状态、异常登录记录。

表关系及说明：

关联关系：引用users.ID做主键。

关系说明：登录态审计、在线状态、异常登录记录需要和其关联的 users.ID保持一致性。

索引说明：

当前以主键或业务唯一键访问为主，暂不单独增加二级索引；如后续出现按状态、时间或主体维度的列表查询，再补充复合索引。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|USER_ID|varchar(128)|PK|用户 id|
|TOKEN|varchar(1024)||登录 token|
|USER_ROLE|varchar(256)||用户角色|
|SERVER_NAME|varchar(20)||服务节点|
|CLIENT_IP|varchar(100)||客户端 IP|
|CLIENT_BROWSER|varchar(1024)||浏览器信息|
|LOGIN_STATUS|decimal(1)||登录状态|
|LOGIN_TIME|datetime||登录时间|
|LOGOUT_TIME|datetime||退出时间|
|LOGIN_ERR_TIMES|decimal(2)||失败次数|
|LOGIN_ERR_MSG|varchar(256)||失败原因|
|ONLINE_TIME|bigint||在线时长|

**user_sign****（****已有****）**

作用：密码签名与摘要表。

对应功能：密码校验、签名管理。

表关系及说明：

关联关系：用users.ID做主键。

关系说明：密码校验、签名管理需要和其关联的 users.ID保持一致性。

索引说明：

user_id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|user_id|varchar(128)|PK|用户 id|
|digest|varchar(128)||摘要|
|last_modified_time|datetime||最后修改时间|

**priv_group****（****已有****）**

作用：用户组表。

对应功能：权限分组、资源授权主体。

表及关系说明：

关联关系：

1. 主键被 priv_group_role_relation.group_id、

priv_user_group_relation.group_id和sa_api_key.group_id引用。

2. sa_resource_acl.subject_id 在 subject_type=GROUP 时引用。

关系说明：权限分组、资源授权主体需要和其关联的 priv_group_role_relation.group_id、priv_user_group_relation.group_id、

sa_api_key.group_id、sa_resource_acl.subject_id、subject_type=GROUP保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(32)|PK|用户组 id|
|name|varchar(255)||组名|
|type|varchar(32)||组类型|
|create_time|datetime||创建时间|
|update_time|datetime||更新时间|
|enable|tinyint(1)||是否启用|
|description|varchar(255)||描述|

**priv_role****（****已有****）**

作用：角色表。

对应功能：功能权限授权。

表关系及说明：

关联关系：主键被 priv_group_role_relation.role_id和

priv_role_permission_relation.role_id引用

关系说明：功能权限授权需要和其关联的 priv_group_role_relation.role_id、priv_role_permission_relation.role_id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

name_index：角色名称普通索引，用于按名称检索角色。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(32)|PK|角色 id|
|name|varchar(255)|IDX|角色名|
|update_time|datetime||更新时间|
|description|varchar(255)||描述|
|is_default|tinyint(1)||是否默认|
|type|varchar(32)||角色类型|
|fid|varchar(32)||父角色 id|

**priv_permission****（****已有****）**

作用：权限点定义表。

对应功能：接口权限、功能点权限控制。

表关系及说明：

关联关系：主键被 priv_role_permission_relation.permission_id和

priv_menu_access.per_id关联。

关系说明：接口权限、功能点权限控制需要和其关联的 priv_role_permission_relation.permission_id、priv_menu_access.per_id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(32)|PK|权限 id|
|service_id|varchar(32)||服务 id|
|name|varchar(255)||权限名|
|permission|varchar(255)||权限编码|
|description|varchar(255)||描述|
|type|varchar(32)||权限类型，MENU/OBJECT|
|is_default|tinyint(1)||是否默认，默认0|

**priv_menu_resource****（****已有****）**

作用：菜单与页面资源表。

对应功能：菜单可见性、页面入口控制。

表关系及说明：

关联关系：主键priv_menu_access.menu_id关联

关系说明：菜单可见性、页面入口控制需要和priv_menu_access.menu_id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(32)|PK|菜单 id|
|service_id|varchar(32)||服务 id|
|type|varchar(32)||菜单类型：0系统内部组件/1自定义菜单|
|seq|||排序|
|name|varchar(255)||菜单名|
|route|varchar(255)||路由|
|icon|varchar(80)||图标|
|params|varchar(4096)||参数|
|description|varchar(255)||描述|
|fid|varchar(32)|FK|父菜单 id|

**priv_menu_access****（****已有****）**

作用：权限点与菜单映射表。

对应功能：控制菜单、页面、按钮元素可见性。

表关系及说明：

关联关系：per_id -> priv_permission.id和menu_id -> priv_menu_resource.id

关系说明：控制菜单、页面、按钮元素可见性需要和其关联的 priv_permission.id、priv_menu_resource.id保持一致性。

索引说明：

PRIMARY KEY(per_id)`：按权限 id 精确定位菜单授权记录。

    menu_index(menu_id, ele_id)：按菜单和元素维度查询授权记录。字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|per_id|varchar(32)|PK|权限 id|
|menu_id|varchar(32)|IDX|菜单 id|
|ele_id|varchar(32)|IDX|元素 id|

**priv_group_role_relation****（****已有****）**

作用：用户组与角色关系表。

对应功能：把角色授权给组。

表关系及说明：

关联关系：group_id -> priv_group.id和role_id -> priv_role.id

关系说明：把角色授权给组需要和其关联的 priv_group.id、priv_role.id保持一致性。

索引说明：

(group_id, role_id)：联合主键，用于去重和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|group_id|varchar(32)|PK，FK|用户组 id|
|role_id|varchar(32)|PK，FK|角色 id|

**priv_role_permission_relation****（****已有****）**

作用：角色与权限点关系表。

对应功能：角色功能权限配置。

表关系及说明：

关联关系：role_id -> priv_role.id和permission_id -> priv_permission.id

关系说明：角色功能权限配置需要和其关联的 priv_role.id、priv_permission.id保持一致性。

索引说明：

(role_id, permission_id)：联合主键，用于去重和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|role_id|varchar(32)|PK，FK|角色 id|
|permission_id|varchar(32)|PK，FK|权限 id|

**priv_user_group_relation****（****已有****）**

作用：用户与用户组关系表。

对应功能：确定用户所属组。

表关系及说明：

关联关系：user_id -> users.ID和group_id -> priv_group.id

关系说明：确定用户所属组需要和其关联的 users.ID、priv_group.id保持一致性。

索引说明：

(user_id, group_id)：联合主键，用于去重和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|user_id|varchar(128)|PK，FK|用户 id|
|group_id|varchar(32)|PK，FK|用户组 id|

**sa_api_key**

作用：SophicAgent API 凭证表。

对应功能：API 调用鉴权、Token 管理。

表关系及说明：

关联关系：group_id -> priv_group.id和user_id -> users.ID

关系说明：API 调用鉴权、Token 管理需要和其关联的 priv_group.id、users.ID保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|group_id|varchar(32)||关联用户组或授权主体|
|user_id|varchar(128)||关联用户|
|api_key|varchar(255)||API Key|
|description|varchar(1024)||描述|
|expired_at|datetime||过期时间|
|status|tinyint||状态|
|gmt_create|datetime||创建时间|
|gmt_modified|datetime||修改时间|

**sa_resource**

作用：统一资源注册表。

对应功能：应用、工具、知识库、数据源、MCP、记忆库等资源统一授权锚点。

表关系及说明：

关联关系：被 sa_resource_acl.resource_id 引用与 sa_application、sa_tool、sa_mcp_server、sa_datasource、sa_memory_library 业务一对一映射

关系说明：应用、工具、知识库、数据源、MCP、记忆库等资源统一授权锚点需要和其关联的 sa_resource_acl.resource_id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|物理主键|
|resource_id|varchar(64)||业务资源 id|
|resource_name|varchar(128)||资源名称|
|resource_type|varchar(32)||资源类型|
|creator_id|varchar(64)||创建人|
|gmt_create|datetime||创建时间|
|gmt_modified|datetime||更新时间|
|owner_user_id|varchar(128)||所有人|
|ext_config|json||扩展配置|
|status|tinyint||状态（是否删除），默认为0|

**sa_resource_acl**

作用：资源访问控制表。

对应功能：资源级读写权限控制。

表关系及说明：

关联关系：resource_id -> sa_resource.resource_id，subject_id 由 subject_type 决定，可能关联 users.ID、priv_group.id。

关系说明：资源级读写权限控制需要和其关联的 sa_resource.resource_id、users.ID、priv_group.id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

(resource_id, subject_type, subject_id, permission_type)：联合索引，用于做资源权限判定，用户访问某个应用、工具、知识库、数据源时，快速判断某主体是否具备 READ/WRITE 权限，是高频鉴权核心索引。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|bigint unsigned|PK|主键|
|resource_id|varchar(64)|IDX|资源 id|
|subject_type|varchar(32)|IDX|主体类型，USER/GROUP|
|subject_id|varchar(64)|IDX|主体 id|
|permission_type|varchar(32)|IDX|权限类型，READ/WRITE|
|grant_type|varchar(32)||授权方式，DIRECT/INHERITED|
|status|tinyint||状态（是否删除），默认为0|
|creator|varchar(64)||创建人|
|modifier|varchar(64)||修改人|
|gmt_create|datetime||创建时间|
|gmt_modified|datetime||修改时间|

### **10.2.2** **应用与设计器域**

**sa_agent_template**

作用：智能体模板表。

对应功能：创建智能体时选择平台提供的智能体模板（问数、报告）。

表关系及说明：

关联关系：id -> sa_application.source

关系说明：在sa_application表中被引用，如果创建应用时选择通过模板创建应用中会带对应的模板id。。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|template_name|varchar(255)||模板名称|
|template_type|varchar(64)||模板类型|
|description|varchar(4096)||描述|
|schema_snapshot|longtext||结构快照|
|config_snapshot|longtext||默认配置快照|
|input_schema|longtext||入参定义|
|output_schema|longtext||出参定义|
|enabled|tinyint||是否启用|
|creator|varchar(64)||创建人|
|modifier|varchar(64)||修改人|
|gmt_create|datetime||创建时间|
|gmt_modified|datetime||修改时间|

**sa_application**

作用：应用主表。

对应功能：智能体/工作流列表、详情、复制、发布、授权。

表关系及说明：

关联关系：

1-N sa_application_version（应用版本管理）

1-N sa_application_binding（应用资源绑定）

1-N sa_guardrail_rule（规则干预）

1-N sa_guardrail_fixed_reply（数据干预）

1-N sa_session（会话）

1-N sa_task（会话任务）

1-N sa_agent_feedback（用户反馈）

N-1 sa_agent_template（智能体模板）

关系说明：智能体/工作流列表、详情、复制、发布、授权需要和相关业务对象保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|name|varchar(255)||应用名称|
|description|varchar(4096)||应用描述|
|source|varchar(64)||来源（当根据模板创建时，为模板id）|
|type|varchar(64)||应用类型，agent/workflow|
|latest_version|varchar(32)||最新版本|
|published_version|varchar(32)||已发布版本|
|status|tinyint||状态（是否删除），默认为0|
|creator|varchar(64)||创建人|
|modifier|varchar(64)||修改人|
|gmt_create|datetime||创建时间|
|gmt_modified|datetime||修改时间|

**sa_application_version**

作用：应用版本表。

对应功能：版本管理、DSL 导出、历史版本查看、发布版本指向。

表关系及说明：

agent_id -> sa_application.id

被 sa_task(agent_id, agent_version)、sa_publish_record(asset_id, asset_version)、sa_eval_task(target_id, target_version)、sa_perf_test_task(target_id, target_version) 逻辑关联。

关系说明：版本管理、DSL 导出、历史版本查看、发布版本指向需要和其关联的 sa_application.id、sa_task(agent_id, agent_version)、sa_publish_record(asset_id, asset_version)、sa_eval_task(target_id, target_version)、

sa_perf_test_task(target_id, target_version)保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

(agent_id, version_type, version)：联合索引，用于定位某个应用的指定版本，用于版本详情、发布指定版本、复制版本。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|bigint unsigned|PK|主键|
|agent_id|varchar(64)|IDX|应用 id|
|version_type|varchar(32)||版本类型（草稿、发布版）|
|version|varchar(32)|IDX|版本号|
|version_desc|varchar(4096)||版本说明|
|config_snapshot|longtext||完整配置快照|
|runtime_config|longtext||运行配置|
|instruction|longtext||Agent 系统提示词，按版本存储|
|input_keys|longtext||Agent 输入键定义，按版本存储|
|output_key|varchar(255)||Agent 输出键，按版本存储|
|handle_config|longtext||Agent 工具配置，按版本存储|
|dsl_schema|longtext||DSL 导出快照|
|change_log|longtext||变更摘要|
|base_version|varchar(32)||派生来源版本|
|status|tinyint||状态（是否删除），默认为0|
|creator|varchar(64)||创建人|
|modifier|varchar(64)||修改人|
|gmt_create|datetime||创建时间|
|gmt_modified|datetime||修改时间|

**sa_application_binding**

作用：应用挂载组件表。

对应功能：把知识库、工具、数据源、记忆等能力挂到应用版本上。

表关系及说明：

关联关系：(agent_id, agent_version) -> sa_application_version(agent_id, version)

resource_id -> sa_resource.resource_id 或对应业务资，对应n个sa_application_binding_item(资源明细表)记录。

关系说明：把知识库、工具、数据源、记忆等能力挂到应用版本上需要和其关联的 (agent_id, agent_version)、sa_application_version(agent_id, version)、sa_resource.resource_id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

(agent_id, agent_version)：联合索引，用于查询某个应用某个版本挂了哪些资源，如工具、知识库、MCP、数据源等。

(resource_id, resource_type, status)：联合索引，用于反向查“某个资源被哪些应用引用”，业务上资源下线、删除前校验、影响面分析、资源复用统计，都要按资源类型和资源 ID 反查绑定关系。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|bigint unsigned|PK|主键|
|binding_id|varchar(64)||绑定 id|
|agent_id|varchar(64)|FK, IDX|应用 id|
|agent_version|varchar(32)|FK, IDX|应用版本|
|resource_type|varchar(64)|IDX|资源类型|
|resource_id|varchar(64)|FK, IDX|资源 id|
|resource_name|varchar(255)||资源名称快照|
|binding_source|varchar(64)||绑定来源|
|sort_no|int||排序号|
|enabled|tinyint||启用状态|
|binding_config|longtext||挂载配置|
|status|tinyint||状态（是否删除），默认为0|
|gmt_create|datetime||创建时间|
|gmt_modified|datetime||修改时间|

**sa_application_binding_item**

作用：挂载组件明细表。

对应功能：指定数据源表/字段、MCP 工具明细等。

表关系及说明：

关联关系：binding_id -> sa_application_binding.binding_id

关系说明：指定数据源表/字段、MCP 工具明细等需要和其关联的 sa_application_binding.binding_id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|bigint unsigned|PK|主键|
|binding_id|varchar(64)|FK|绑定 id|
|item_type|varchar(64)||明细类型|
|item_id|varchar(128)||明细项 id|
|item_name|varchar(255)||名称|
|item_config|longtext||明细配置|
|status|tinyint||状态（是否删除），默认为0|
|gmt_create|datetime||创建时间|
|gmt_modified|datetime||修改时间|

**sa_high_code_service**

作用：高代码服务主表。

对应功能：高代码服务注册、列表、版本管理入口。

表关系及说明：

关联关系：

1-N sa_high_code_service_version（高代码服务版本）

1-N sa_high_code_deploy_record（高代码服务部署记录）

1-N sa_high_code_operation_log（高代码服务操作记录）

主键id与smc_service.id 一致，smc_service是服务表包含高代码服务。

关系说明：高代码服务注册、列表、版本管理入口需要和其关联的 smc_service.id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|service_code|varchar(64)||服务编码|
|name|varchar(255)||名称|
|service_type|varchar(64)||服务类型（平台和业务）|
|owner_user_id|varchar(128)||所有人|
|latest_version|varchar(32)||最新版本|
|status|tinyint||状态（是否删除），默认为0|

**sa_high_code_service_version**

作用：高代码服务版本表。

对应功能：jar/docker 包与部署配置管理。

表关系及说明：

关联关系：

service_id -> sa_high_code_service.id

被 sa_high_code_deploy_record(service_id, service_version) 逻辑引用

被 sa_publish_record(asset_id, asset_version) 逻辑引用

关系说明：jar/docker包与部署配置管理需要和其关联的 sa_high_code_service.id、sa_high_code_deploy_record(service_id,

service_version)、sa_publish_record(asset_id, asset_version)保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|service_id|varchar(64)|FK|服务 id|
|version|varchar(32)||版本号|
|package_type|varchar(32)||包类型|
|package_uri|varchar(512)||包地址|
|deploy_config|longtext||部署配置|
|input_schema|longtext||入参定义|
|output_schema|longtext||出参定义|
|change_log|longtext||变更摘要|
|status|tinyint||状态（是否删除），默认为0|

**sa_guardrail_rule**

作用：规则干预配置表。

对应功能：输入/输出敏感词、正则替换、规则过滤。

表关系及说明：

关联关系：

agent_id -> sa_application.id

关系说明：输入/输出敏感词、正则替换、规则过滤需要和其关联的 sa_application.id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|agent_id|varchar(64)|FK|应用 id|
|rule_code|varchar(64)||规则名|
|match_config|longtext||匹配配置|
|description|varchar(1024)||规则说明|
|priority|int||优先级|
|enabled|tinyint||是否启用|
|status|tinyint||状态（是否删除），默认为0|

**sa_guardrail_fixed_reply**

作用：固定问题回复配置表。

对应功能：数据干预、相似问题固定回复。

表关系及说明：

关联关系：

agent_id -> sa_application.id

关系说明：数据干预、相似问题固定回复需要和其关联的 sa_application.id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|agent_id|varchar(64)|FK|应用 id|
|input_snapshot|longtext||问题配置|
|output_snapshot|longtext||输出配置|
|priority|int||优先级|
|enabled|tinyint||是否启用|
|status|tinyint||状态（是否删除），默认为0|
|gmt_create|datetime||创建时间|
|gmt_modified|datetime||修改时间|

### **10.2.3** **运行时域**

**sa_session**

作用：会话主表。

对应功能：会话管理、会话列表、最近活跃记录。

表关系及说明：

agent_id -> sa_application.id

user_id -> users.ID

1-N sa_session_message（对话记录表）

1-N sa_task（智能体或工作流单轮执行任务表）

1-N sa_memory_short_term（短期记忆表）

关系说明：会话管理、会话列表、最近活跃记录需要和其关联的 sa_application.id、users.ID保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

(user_id, gmt_last_active)：用于查询某个用户最近会话列表，对话记录通常都是“按用户筛选，按最后活跃时间倒序展示”。

(agent_id, status, gmt_last_active)：用于查看某个应用的会话运行情况，运维数据统计时使用。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|agent_id|varchar(64)|FK, IDX|应用 id|
|agent_version|varchar(32)||应用版本|
|user_id|varchar(128)|FK, IDX|用户 id|
|session_title|varchar(255)||会话标题|
|source_type|varchar(64)||会话来源|
|gmt_last_active|datetime|IDX|最后活跃时间|
|status|tinyint||状态|
|gmt_create|datetime||创建时间|
|gmt_modified|datetime||修改时间|

**sa_session_message**

作用：会话消息表。

对应功能：对话日志、消息追踪、上下文恢复。

表关系及说明：

session_id -> sa_session.id

被 sa_task.trigger_message_id 与 sa_task.response_message_id 引用

关系说明：对话日志、消息追踪、上下文恢复需要和其关联的 sa_session.id、sa_task.trigger_message_id、sa_task.response_message_id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

(session_id, seq_no)：用于按顺序加载一个会话的消息流。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|session_id|varchar(64)|FK，IDX|会话 id|
|seq_no|int|IDX|顺序号|
|role|varchar(32)||消息角色|
|content|longtext||消息内容|
|content_type|varchar(32)||内容类型|
|token_usage|json||token 消耗|
|metadata|json||扩展信息|
|gmt_create|datetime||创建时间|

**sa_task**

作用：智能**sa_task**

体或工作流单次执行任务（和对话绑定，单次执行流程总览表）。

对应功能：智能体本轮执行、运行态任务追踪。

表关系及说明：

session_id -> sa_session.id

trigger_message_id -> sa_session_message.id

response_message_id -> sa_session_message.id

(agent_id, agent_version) 逻辑关联 sa_application_version

1-N sa_execution_step（智能体或工作流单轮中执行步骤）

1-N sa_invoke_log（智能体和工作流调用日志）

1-N sa_memory_hit_record（记忆命中记录）

1-N sa_agent_feedback（智能体反馈）

关系说明：智能体本轮执行、运行态任务追踪需要和其关联的 sa_session.id、sa_session_message.id、(agent_id, agent_version)保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

(session_id, gmt_start)：用于查询一个会话下的执行任务历史。

(agent_id, agent_version, status, gmt_start)：从应用维度查看任务执行情况，用于运行监控、按版本排查问题、查看某版本失败任务、统计某应用某状态下的执行记录。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|session_id|varchar(64)|FK, IDX|会话 id|
|trigger_message_id|varchar(64)|FK|触发消息（用户问题）|
|response_message_id|varchar(64)|FK|响应消息 （模型回答）|
|agent_id|varchar(64)|FK, IDX|应用 id|
|agent_version|varchar(32)|IDX|应用版本|
|task_name|varchar(255)||任务名称|
|input_payload|longtext||输入数据|
|output_payload|longtext||输出数据|
|final_result|longtext||最终结果|
|error_code|varchar(64)||错误码|
|error_message|varchar(1024)||错误信息|
|gmt_start|datetime|IDX|开始时间|
|gmt_end|datetime||结束时间|
|status|tinyint||状态（是否删除），默认为0|

**sa_high_code_deploy_record**

作用：高代码部署记录表。

对应功能：部署、回滚、重部署记录。

表关系及说明：

关联关系：(service_id, service_version) 逻辑关联 sa_high_code_service_version

关系说明：部署、回滚、重部署记录需要和其关联的 (service_id, service_version)保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|service_id|varchar(64)|FK|服务 id|
|service_version|varchar(32)||服务版本|
|package_type|varchar(32)||包类型|
|package_uri|varchar(512)||包存储地址|
|deploy_env|varchar(32)||部署环境|
|deploy_type|varchar(32)||部署类型|
|deploy_config|longtext||配置快照|
|operator_id|varchar(128)||操作人|
|result_status|varchar(32)||执行结果|
|result_message|varchar(1024)||结果说明|
|gmt_start|datetime||开始时间|
|gmt_end|datetime||结束时间|
|gmt_create|datetime||创建时间|

**sa_high_code_operation_log**

作用：高代码操作流水表。

对应功能：启动、停止、重启、下线。

表关系及说明：

service_id -> sa_high_code_service.service_id

instance_id -> smc_instance.instance_id

关系说明：启动、停止、重启、扩缩容、下线需要和其关联的 sa_high_code_service.service_id、smc_instance.instance_id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|service_id|varchar(64)|FK|服务 id|
|instance_id|varchar(64)|FK|实例 id|
|operation_type|varchar(32)||操作类型|
|operator_id|varchar(128)||操作人|
|result_status|varchar(32)||结果状态|
|gmt_create|datetime||创建时间|

**sa_execution_step**

作用：执行步骤明细表。

对应功能：流程跟踪、调试链路、步骤级留痕。

表关系及说明：

关联关系：task_id -> sa_task.id

关系说明：流程跟踪、调试链路、步骤级留痕需要和其关联的 sa_task.id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

(task_id, step_no)：用于执行链路回放、工作流调试、Agent 推理步骤查看，都是按任务取步骤并按步骤号排序。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|task_id|varchar(64)|FK，IDX|任务 id|
|step_no|int|IDX|步骤序号|
|step_type|varchar(64)||步骤类型|
|step_name|varchar(255)||步骤名称|
|step_desc|varchar(1024)||步骤说明|
|tool_ref|varchar(128)||工具引用|
|input_snapshot|longtext||输入快照|
|output_snapshot|longtext||输出快照|
|thought_snapshot|longtext||推理过程快照|
|cost_ms|bigint||耗时|
|error_code|varchar(64)||错误码|
|error_message|varchar(1024)||错误信息|
|status|tinyint||状态（是否删除），默认为0|

**smc_application****（****smc服务****）**

作用：运行应用表。

对应功能：高代码运行平台应用归属。

表关系及说明：

关联关系：1-N smc_service.ref_application（smc通用服务表）

关系说明：高代码运行平台应用归属需要和其关联的 smc_service.ref_application保持一致性。

索引说明：

application_id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|application_id|varchar(32)|PK|应用 id|
|application_name|varchar(50)||应用名|
|owner|varchar(32)||负责人|
|create_time|timestamp||创建时间|
|note|varchar(255)||描述|
|seq|int||排序|

**smc_service（****smc服务****）**

作用：通用服务表。

对应功能：服务发现、运行态服务映射。

表关系及说明：

ref_application -> smc_application.application_id

1-N smc_instance.service_id（服务实例表）

关系说明：服务发现、运行态服务映射需要和其关联的 smc_application.application_id、smc_instance.service_id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|服务 id|
|service_name|varchar(255)||服务名称|
|source|varchar(32)||来源|
|params|varchar(1024)||启动参数|
|type|varchar(32)||服务类型|
|create_time|datetime||创建时间|
|update_time|datetime||更新时间|
|note|varchar(255)||描述|
|version|varchar(255)||版本|
|service_info|varchar(50)||服务信息|
|ref_application|varchar(50)|FK|归属应用|
|seq|int||排序|

**smc_instance（****smc服务****）**

作用：服务实例表。

对应功能：实例状态管理、运行控制。

表关系及说明：

service_id -> smc_service.id

被 sa_high_code_operation_log.instance_id 引用

关系说明：实例状态管理、运行控制需要和其关联的 smc_service.id、sa_high_code_operation_log.instance_id保持一致性。

索引说明：

instance_id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|instance_id|varchar(32)|PK|实例 id|
|service_id|varchar(255)|FK|服务 id|
|ip|varchar(64)||IP|
|port|int||端口|
|type|varchar(64)||类型|
|instance_mode|varchar(64)||运行模式|
|fingerprint|varchar(64)||指纹|
|enabled|tinyint(1)||是否可用|
|update_time|datetime||更新时间|
|insert_time|datetime||插入时间|
|version|varchar(50)||版本|
|last_offline_time|datetime||最近离线时间|
|weight|int||权重|

### **10.2.4** **能力资产域**

**sa_tool**

作用：工具主表。

对应功能：工具中心管理、应用/工作流发布为工具后的承载。

表关系及说明：

1-N sa_tool_version（工具版本快照，智能体和工作流发布为工具）

1-N sa_tool_debug_record（工具调试记录）

1-1 sa_resource（资源表）

publish_id -> sa_publish_record.id

关系说明：工具中心管理、应用/工作流发布为工具后的承载需要和其关联的 sa_publish_record.id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|name|varchar(255)||工具名称|
|description|varchar(4096)||描述|
|tool_type|varchar(64)||工具类型|
|source_asset_type|varchar(32)||来源资产类型（agent、workflow）|
|source_asset_id|varchar(64)||来源资产 id|
|source_asset_version|varchar(32)||来源版本|
|publish_id|varchar(64)|FK|发布记录 id|
|api_schema|longtext||接口 schema|
|config|longtext||配置|
|test_status|tinyint||测试状态|
|enabled|tinyint||是否启用|
|latest_version|varchar(32)||最新版本|
|status|tinyint||状态（是否删除），默认为0|
|gmt_create|datetime||创建时间|
|gmt_modified|datetime||修改时间|
|creator|varchar(64)||创建人|
|modifier|varchar(64)||修改人|

**sa_tool_version**

作用：工具版本表。

对应功能：工具版本管理。

表关系及说明：

tool_id -> sa_tool.id

被 sa_publish_record(asset_id, asset_version) 逻辑引用

关系说明：工具版本管理需要和其关联的 sa_tool.id、sa_publish_record(asset_id, asset_version)保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|bigint unsigned|PK|主键|
|tool_id|varchar(64)|FK|工具 id|
|version|varchar(32)||版本号|
|version_desc|varchar(4096)||版本说明|
|schema_snapshot|longtext||schema 快照|
|config_snapshot|longtext||配置快照|
|status|tinyint||状态（是否删除），默认为0|
|gmt_create|datetime||创建时间|

**sa_tool_debug_record**

作用：工具调试记录表。

对应功能：工具调试、联调日志。

表关系及说明：

关联关系：tool_id -> sa_tool.id

关系说明：工具调试、联调日志需要和其关联的 sa_tool.id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|bigint unsigned|PK|主键|
|tool_id|varchar(64)|FK|工具 id|
|request_payload|json/longtext||调试请求|
|response_payload|json/longtext||调试响应|
|success|tinyint||是否成功|
|error_message|varchar(1024)||错误信息|
|cost_ms|bigint||耗时|
|gmt_create|datetime|IDX|创建时间|

**sa_mcp_server**

作用：MCP 服务主表。

对应功能：MCP 服务注册、配置、启停、权限控制。

表关系及说明：

1-N sa_mcp_server_instance（mcp服务实例）

1-1 sa_resource（资源表）

关系说明：MCP 服务注册、配置、启停、权限控制需要和相关业务对象保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|server_code|varchar(64)||服务编码|
|name|varchar(64)||服务名称|
|description|varchar(1024)||描述|
|source|varchar(128)||来源（nacos/higress）|
|deploy_env|varchar(16)||部署环境|
|deploy_config|text||部署配置|
|user_id|varchar(128)||所有者|
|status|tinyint||状态|
|detail_config|text||详情配置|
|tool_schema|longtext||工具 schema|
|host|varchar(1024)||服务地址|
|install_type|varchar(32)||安装方式（docker/jar）|
|gmt_create|datetime||创建时间|
|gmt_modified|datetime||修改时间|
|creator|varchar(64)||创建人|
|modifier|varchar(64)||修改人|

**sa_mcp_server_instance**

作用：MCP 服务实例表。

对应功能：实例列表、心跳、健康状态。

表关系及说明：

关联关系：server_code -> sa_mcp_server.server_code

关系说明：实例列表、心跳、健康状态需要和其关联的 sa_mcp_server.server_code保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|server_code|varchar(64)|FK|服务编码|
|instance_name|varchar(255)||实例名称|
|endpoint|varchar(1024)||访问地址|
|health_status|varchar(32)||健康状态|
|runtime_status|varchar(32)||运行状态|
|metadata|longtext||扩展信息|

**sa_memory_short_term**

作用：短期记忆表。

对应功能：多轮上下文保留。

表关系及说明：

关联关系：session_id -> sa_session.id

关系说明：多轮上下文保留需要和其关联的 sa_session.id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|session_id|varchar(64)|FK|会话 id|
|seq_no|int||对话轮次|
|role|varchar(32)||角色|
|content|longtext||记忆内容|
|metadata|json||扩展信息|
|expired_at|datetime||过期时间|

**sa_memory_library**

作用：记忆库主表。

对应功能：长期记忆库管理。

表关系及说明：

1-N sa_memory_rule（记忆规则）

1-N sa_memory_long_term（长期记忆明细）

1-N sa_memory_entity（记忆实体）

1-N sa_memory_recall_test（记忆召回测试记录）

1-1 sa_resource（资源表）

关系说明：长期记忆库管理需要和相关业务对象保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|name|varchar(255)||名称|
|description|varchar(4096)||描述|
|owner_user_id|varchar(128)||所有人|
|status|tinyint||状态（是否删除），默认为0|

**sa_memory_rule**

作用：记忆规则表。

对应功能：长期记忆提取规则、过期策略。

表关系及说明：

关联关系：memory_library_id -> sa_memory_library.id

关系说明：长期记忆提取规则、过期策略需要和其关联的 sa_memory_library.id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|memory_library_id|varchar(64)|FK|记忆库 id|
|rule_name|varchar(255)||规则名称|
|expire_days|int||过期天数|
|rule_content|json/longtext||规则内容|
|enabled|tinyint||是否启用|
|status|tinyint||状态|

**sa_memory_long_term**

作用：长期记忆表。

对应功能：记忆片段存储、召回、命中统计。

表关系及说明：

memory_library_id -> sa_memory_library.id

agent_id -> sa_application.id

entity_id -> sa_memory_entity.id

session_id -> sa_session.id

rule_id -> sa_memory_rule.id

1-N sa_memory_hit_record（记忆命中记录）

关系说明：记忆片段存储、召回、命中统计需要和其关联的 sa_memory_library.id、sa_application.id、users.ID、sa_memory_entity.id、sa_session.id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

(memory_library_id, user_id, status, gmt_create)：用于按记忆库查长期记忆，并按时间查看最新记录。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|memory_library_id|varchar(64)|FK|记忆库 id|
|agent_id|varchar(64)|FK|应用 id|
|entity_id|varchar(64)|FK|记忆实体 id|
|session_id|varchar(64)|FK|会话 id|
|rule_id|varchar(64)|FK|规则id|
|content|longtext||原始内容|
|summary_content|text||摘要内容|
|expired_at|datetime||过期时间|
|hit_count|int||命中次数|
|last_hit_time|datetime||最后命中时间|
|status|tinyint||状态|

**sa_memory_hit_record**

作用：记忆命中记录表。

对应功能：记忆召回日志、命中率统计。

表关系及说明：

memory_library_id -> sa_memory_library.id

memory_id -> sa_memory_long_term.id

task_id -> sa_task.id

关系说明：记忆召回日志、命中率统计需要和其关联的 sa_memory_library.id、sa_memory_long_term.id、sa_task.id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|memory_library_id|varchar(64)|FK|记忆库 id|
|memory_id|varchar(64)|FK|记忆 id|
|task_id|varchar(64)|FK|任务 id|
|query_text|text||查询文本|
|similarity_score|decimal(10,4)||相似度|
|status|tinyint||状态|

**sa_memory_entity**

作用：记忆实体表。

对应功能：按应用维度聚合记忆。

表关系及说明：

memory_library_id -> sa_memory_library.id

agent_id -> sa_application.id

1-N sa_memory_long_term（长期记忆记录）

关系说明：按应用维度聚合记忆需要和其关联的 sa_memory_library.id、sa_application.id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|memory_library_id|varchar(64)|FK|记忆库 id|
|agent_id|varchar(64)|FK|应用 id|
|entity_type|varchar(64)||实体类型|
|entity_name|varchar(255)||实体名称|
|entity_summary|text||摘要|
|metadata|text||扩展信息|
|status|tinyint||状态|

**sa_memory_recall_test**

作用：记忆召回测试表。

对应功能：记忆检索测试、命中率评估。

表关系及说明：

memory_library_id -> sa_memory_library.id

expected_memory_id 逻辑关联 sa_memory_long_term.id

关系说明：记忆检索测试、命中率评估需要和其关联的 sa_memory_library.id、sa_memory_long_term.id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|memory_library_id|varchar(64)|FK|记忆库 id|
|query_text|text||测试查询|
|expected_memory_id|varchar(64)||期望命中记忆 id|
|actual_result|longtext||实际结果|
|score_detail|text||评分明细|
|status|tinyint||状态|

**sa_datasource**

作用：数据源主表。

对应功能：数据源管理、筛选、搜索、概览统计。

表关系及说明：

1-N sa_datasource_table（数据表元数据）

1-N sa_datasource_field（数据表属性元数据）

1-N sa_datasource_relation（逻辑外键）

1-N sa_datasource_semantic_model（语义模型）

与 sa_resource（资源表） 一对一映射

关系说明：数据源管理、筛选、搜索、概览统计需要和相关业务对象保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

datasource_name：用于数据源管理页模糊/精确搜索、配置绑定时按名称快速定位目标数据源。

(datasource_type, schema_type, datasource_status)：用于只看某类库、某种 schema 模式、当前可用/不可用的数据源。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|datasource_name|varchar(255)|IDX|数据源名称|
|schema_type|varchar(64)|IDX|模式类型|
|datasource_type|varchar(64)|IDX|数据源类型|
|host|varchar(255)||主机|
|port|int||端口|
|database_name|varchar(255)||数据库名|
|username|varchar(255)||用户名|
|password|varchar(512)||密码密文|
|connection_url|varchar(1024)||连接串|
|description|varchar(512)||数据源描述|
|datasource_status|tinyint|IDX|状态|
|gmt_create|datetime||新建时间|
|gmt_modified|datetime||最后同步时间|

**sa_datasource_table**

作用：数据表元数据表。

对应功能：展示数据源下的表清单。

表关系及说明：

datasource_id -> sa_datasource.id

1-N sa_datasource_field（数据表属性）

被 sa_datasource_relation.source_table_id/target_table_id

被 sa_datasource_semantic_model.table_id

关系说明：展示数据源下的表清单需要和其关联的 sa_datasource.id、sa_datasource_relation.source_table_id/target_table_id、sa_datasource_semantic_model.table_id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|bigint unsigned|PK|主键|
|datasource_id|varchar(64)|FK|数据源 id|
|schema_name|varchar(255)||schema 名|
|table_name|varchar(255)||表名|
|table_comment|varchar(1024)||表说明|
|refresh_version|varchar(64)||刷新版本|
|is_deleted|tinyint||逻辑删除|
|status|tinyint||状态|

**sa_datasource_field**

作用：数据字段元数据表。

对应功能：字段选择、字段语义配置入口。

表关系及说明：

datasource_id -> sa_datasource.id

table_id -> sa_datasource_table.id

被 sa_datasource_semantic_model.field_id

关系说明：字段选择、字段语义配置入口需要和其关联的 sa_datasource.id、sa_datasource_table.id、sa_datasource_semantic_model.field_id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|datasource_id|varchar(64)|FK|数据源 id|
|table_id|varchar(64)|FK|表 id|
|field_id|varchar(64)|UQ|字段 id|
|column_name|varchar(255)||字段名|
|column_comment|varchar(1024)||字段说明|
|data_type|varchar(64)||数据类型|
|is_primary|tinyint||是否主键|
|is_foreign|tinyint||是否外键|
|is_not_null|tinyint||是否非空|
|field_status|varchar(64)||字段状态|
|refresh_version|varchar(64)||刷新版本|
|is_deleted|tinyint||逻辑删除|
|status|tinyint||状态|

**sa_datasource_relation**

作用：数据源逻辑关系表。

对应功能：逻辑外键配置。

表关系及说明：

datasource_id -> sa_datasource.id

source_table_id -> sa_datasource_table.id

target_table_id -> sa_datasource_table.id

关系说明：逻辑外键配置需要和其关联的 sa_datasource.id、sa_datasource_table.id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|datasource_id|varchar(64)|FK|数据源 id|
|source_table_id|varchar(64)|FK|源表 id|
|target_table_id|varchar(64)|FK|目标表 id|
|source_field_name|varchar(255)||源字段名|
|target_field_name|varchar(255)||目标字段名|
|relation_type|varchar(64)||关系类型|
|description|varchar(1024)||描述|
|status|tinyint||状态|

**sa_datasource_semantic_model**

作用：数据源语义配置表。

对应功能：字段业务语义、同义词、业务描述配置。

表关系及说明：

datasource_id -> sa_datasource.id

table_id -> sa_datasource_table.id

field_id -> sa_datasource_field.id

关系说明：字段业务语义、同义词、业务描述配置需要和其关联的 sa_datasource.id、sa_datasource_table.id、sa_datasource_field.id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|datasource_id|varchar(64)|FK|数据源 id|
|table_id|varchar(64)|FK|表 id|
|field_id|varchar(64)|FK|字段 id|
|semantic_level|varchar(64)||语义层级|
|model_name|varchar(255)||模型名称|
|field_name|varchar(255)||展示字段名|
|business_name|varchar(255)||业务名称|
|synonyms|json||同义词|
|business_description|text||业务描述|
|metadata|json||扩展信息|
|status|tinyint||状态|

### **10.2.5** **治理观测与运营域**

**sa_model_provider**

作用：模型供应商表。

对应功能：模型供应商管理。

表关系及说明：

1-N sa_model（模型表）

1-N sa_model_config（模型配置表）

关系说明：模型供应商管理需要和相关业务对象保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|bigint unsigned|PK|主键|
|provider_code|varchar(64)||供应商编码|
|provider_key|varchar(255)||供应商唯一标识|
|name|varchar(255)||名称|
|description|varchar(1024)||描述|
|supported_model_types|text||支持模型类型|
|enabled|tinyint||是否启用|
|status|tinyint||状态|
|gmt_create|datetime||创建时间|
|gmt_modified|datetime||修改时间|
|creator|varchar(64)||创建人|
|modifier|varchar(64)||修改人|

**sa_model**

作用：模型定义表。

对应功能：模型清单管理。

表关系及说明：

provider_code -> sa_model_provider.provider_code

1-N sa_model_config（模型配置）

可被 sa_tag_binding（标签绑定表） 绑定标签

关系说明：模型清单管理需要和其关联的 sa_model_provider.provider_code保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|bigint unsigned|PK|主键|
|model_id|varchar(100)||模型标识|
|provider_code|varchar(64)|FK|供应商编码|
|model_code|varchar(64)||模型编码|
|name|varchar(255)||模型名称|
|model_type|varchar(64)||模型类型|
|enabled|tinyint||是否启用|
|status|tinyint||状态|
|gmt_create|datetime||创建时间|
|gmt_modified|datetime||修改时间|
|creator|varchar(64)||创建人|
|modifier|varchar(64)||修改人|

**sa_model_config**

作用：模型接入配置表。

对应功能：模型运行参数、地址、鉴权配置。

表关系及说明：

provider_code -> sa_model_provider.provider_code

model_id -> sa_model.model_id

被应用版本快照逻辑引用

关系说明：模型运行参数、地址、鉴权配置需要和其关联的 sa_model_provider.provider_code、sa_model.model_id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|config_name|varchar(100)||配置名称|
|provider_code|varchar(64)|FK|供应商编码|
|model_id|varchar(100)|FK|模型 id|
|base_url|varchar(500)||服务地址|
|api_key_cipher|varchar(500)||API Key 密文|
|supported_parameters|text||支持参数定义|
|enabled|tinyint||是否启用|
|status|tinyint||状态|
|gmt_create|datetime||创建时间|
|gmt_modified|datetime||修改时间|
|creator|varchar(64)||创建人|
|modifier|varchar(64)||修改人|

**sa_invoke_log**

作用：调用日志表。

对应功能：应用/工具/MCP/模型调用日志追踪。

表关系及说明：

task_id -> sa_task.id

agent_id -> sa_application.id

关系说明：应用/工具/MCP/模型调用日志追踪需要和其关联的 sa_task.id、sa_application.id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

(task_id, gmt_create)：用于排查一次执行过程里调用了哪些模型、工具、MCP 服务，以及调用先后顺序。

(agent_id, invoke_type, gmt_create)：用于分析某应用的模型调用、工具调用、外部依赖调用，便于做问题排查和成本分析。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|bigint unsigned|PK|主键|
|task_id|varchar(64)|FK|任务 id|
|agent_id|varchar(64)|FK|应用 id|
|invoke_type|varchar(64)||调用类型|
|input_digest|varchar(512)||输入摘要|
|output_digest|varchar(512)||输出摘要|
|error_code|varchar(64)||错误码|
|error_message|varchar(1024)||错误信息|
|cost_ms|bigint||耗时|
|status|tinyint||状态|
|gmt_create|datetime||创建时间|

**sa_runtime_metric**

作用：运行指标表。

对应功能：监测大盘、折线图指标展示。

表关系及说明：

关联关系：与应用、工具、高代码服务通过 target_type + target_id + target_version 逻辑关联

关系说明：监测大盘、折线图指标展示需要和其关联的 target_type + target_id + target_version保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

(target_type, target_id, metric_name, collect_time)：用于查看某应用/工具/服务的 QPS、延迟、成功率等指标趋势图。

(target_type, target_id, target_version, stat_granularity, collect_time)：用于用于按版本和统计粒度看指标。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|bigint unsigned|PK|主键|
|target_type|varchar(64)|IDX|对象类型（应用/高代码/工具）|
|target_id|varchar(64)|IDX|对象 id|
|target_version|varchar(32)|IDX|对象版本|
|metric_name|varchar(128)|IDX|指标名|
|metric_value|decimal(20,4)||指标值|
|metric_unit|varchar(32)||单位|
|stat_granularity|varchar(32)|IDX|粒度（分/时/天/月/年）|
|collect_time|datetime|IDX|采集时间|
|gmt_create|datetime||创建时间|

**sa_agent_feedback**

作用：应用反馈表。

对应功能：意见反馈、评分、问题回溯。

表关系及说明：

agent_id -> sa_application.id

task_id -> sa_task.id

session_id -> sa_session.id

user_id -> users.ID

关系说明：意见反馈、评分、问题回溯需要和其关联的 sa_application.id、sa_task.id、sa_session.id、users.ID保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|agent_id|varchar(64)|FK|应用 id|
|task_id|varchar(64)|FK|任务 id|
|session_id|varchar(64)|FK|会话 id|
|user_id|varchar(128)|FK|用户 id|
|rating|int||评分|
|feedback_type|varchar(64)||反馈类型|
|content|text||反馈内容|
|status|tinyint||状态|
|gmt_create|datetime||创建时间|

**sa_tag**

作用：标签主表。

对应功能：标签管理。

表关系及说明：

关联关系：1-N sa_tag_binding

关系说明：标签管理需要和相关业务对象保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|tag_name|varchar(128)||标签名称|
|tag_category|varchar(64)||标签分类|
|scope_type|varchar(64)||作用域（部门/所）|
|owner_user_id|varchar(128)||所有人|
|description|varchar(1024)||说明|
|status|tinyint||状态|
|gmt_create|datetime||创建时间|
|gmt_modified|datetime||修改时间|

**sa_tag_binding**

作用：标签绑定关系表。

对应功能：标签与资源多对多关联。

表关系及说明：

tag_id -> sa_tag.id

target_id 按 target_type 逻辑关联模板、应用、工具、MCP、数据源、知识库、记忆库、模型、评测样本等

关系说明：标签与资源多对多关联需要和其关联的 sa_tag.id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|bigint unsigned|PK|主键|
|tag_id|varchar(64)|FK|标签 id|
|target_type|varchar(64)||绑定对象类型（应用/工具/MCP/评测集）|
|target_id|varchar(64)||绑定对象 id|
|target_version|varchar(32)||对象版本|
|status|tinyint||状态|
|gmt_create|datetime||创建时间|
|gmt_modified|datetime||修改时间|

**sa_publish_record**

作用：发布记录表。

对应功能：发布为应用、发布为工具、高代码发布记录。

表关系及说明：

被 sa_tool.publish_id 引用

(asset_type, asset_id, asset_version) 逻辑关联应用版本、工具版本。

关系说明：发布为应用、发布为工具需要和其关联的 sa_resource.resource_id、sa_tool.publish_id、(asset_type, asset_id, asset_version)保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

(asset_type, asset_id, asset_version, gmt_create)：用于查询某应用/工具某版本的发布历史。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|asset_type|varchar(64)|IDX|资产类型（应用/工具）|
|asset_id|varchar(64)|IDX|资产 id|
|asset_version|varchar(32)|IDX|资产版本|
|publish_type|varchar(64)||发布类型（发布应用/工具）|
|input_schema|text||入参定义|
|output_schema|text||出参定义|
|config_snapshot|longtext||配置快照|
|operator_id|varchar(128)||操作人|
|status|tinyint||状态|
|gmt_create|datetime|IDX|发布时间|
|gmt_modified|datetime||变更时间|

**sa_eval_dataset**

作用：评测集主表。

对应功能：评测集管理。

表关系及说明：

1-N sa_eval_dataset_item（评测集样本明细）

1-N sa_eval_task（评测任务）

关系说明：评测集管理需要和相关业务对象保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|dataset_id|varchar(64)||评测集 id|
|dataset_name|varchar(255)||名称|
|dataset_type|varchar(64)||类型|
|description|varchar(4096)||描述|
|owner_user_id|varchar(128)||所有人|
|status|tinyint||状态|

**sa_eval_dataset_item**

作用：评测集样本表。

对应功能：一问一答或多轮样本管理。

表关系及说明：

dataset_id -> sa_eval_dataset.id

被 sa_eval_result.dataset_item_id 引用

可被 sa_tag_binding 以 target_type=EVAL_DATASET_ITEM 绑定标签

关系说明：一问一答或多轮样本管理需要和其关联的 sa_eval_dataset.id、sa_eval_result.dataset_item_id、target_type=EVAL_DATASET_ITEM保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|dataset_id|varchar(64)|FK|评测集 id|
|question|text||问题|
|reference_answer|text||参考答案|
|conversation_context|longtext||多轮上下文|

**sa_eval_task**

作用：评测任务表。

对应功能：自动评测、手动评测任务管理。

表关系及说明：

dataset_id -> sa_eval_dataset.id

1-N sa_eval_result

(target_type, target_id, target_version) 逻辑关联应用版本

关系说明：自动评测、手动评测任务管理需要和其关联的 sa_eval_dataset.id、(target_type, target_id, target_version)保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|task_name|varchar(255)||任务名称|
|eval_type|varchar(64)||评测类型|
|target_type|varchar(64)||目标类型（应用/工具/高代码）|
|target_id|varchar(64)||目标 id|
|target_version|varchar(32)||目标版本|
|dataset_id|varchar(64)|FK|评测集 id|
|eval_config|text||评测配置|
|operator_id|varchar(128)||操作人|
|gmt_start|datetime||开始时间|
|gmt_end|datetime||结束时间|
|status|tinyint||状态|

**sa_eval_result**

作用：评测结果表。

对应功能：样本级评分结果记录。

表关系及说明：

eval_task_id -> sa_eval_task.id

dataset_item_id -> sa_eval_dataset_item.id

关系说明：样本级评分结果记录需要和其关联的 sa_eval_task.id、sa_eval_dataset_item.id保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

(eval_task_id, dataset_item_id)：用于定位某次评测任务下某条样本的结果。。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|eval_task_id|varchar(64)|FK, IDX|评测任务 id|
|dataset_item_id|varchar(64)|FK, IDX|样本 id|
|question|text||问题|
|reference_answer|text||参考答案|
|generated_answer|text||生成答案|
|auto_score|decimal(10,4)||自动评分|
|manual_score|decimal(10,4)||人工评分|
|score_detail|json||评分明细|
|status|tinyint||状态|

**sa_perf_test_task**

作用：压测任务表。

对应功能：应用/工具性能评估任务管理。

表关系及说明：

关联关系：(target_type, target_id, target_version) 逻辑关联应用、工具或高代码服务版本

关系说明：应用/工具性能评估任务管理需要和其关联的 (target_type, target_id, target_version)保持一致性。

索引说明：

id：主键索引，用于单条记录精确定位和主从关联。

字段明细表：

|   |   |   |   |
|---|---|---|---|
|**属性名**|**类型**|**约束**|**说明**|
|id|varchar(64)|PK|主键|
|task_name|varchar(255)||任务名称|
|target_type|varchar(64)||压测目标类型（应用/高代码/工具）|
|target_id|varchar(64)||目标 id|
|target_version|varchar(32)||目标版本|
|concurrency_level|int||并发数|
|request_count|int||请求总数|
|perf_config|text||压测配置|
|result_summary|text||结果摘要|
|operator_id|varchar(128)||操作人|
|gmt_start|datetime||开始时间|
|gmt_end|datetime||结束时间|
|status|tinyint||状态|